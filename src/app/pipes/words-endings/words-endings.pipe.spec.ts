import { WordsEndingsPipe } from './words-endings.pipe';

describe('WordsEndingsPipe', () => {
  it('create an instance', () => {
    const pipe = new WordsEndingsPipe();
    expect(pipe).toBeTruthy();
  });
});
